<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
	<!-- Throttle, authorize, validate, cache, or transform the requests -->
	<inbound>
		<base />
		<validate-content unspecified-content-type-action="prevent" max-size="124000" size-exceeded-action="ignore" errors-variable-name="Validation_Error">
			<content type="application/json" validate-as="json" schema-id="create-proj-schema" action="prevent" />
		</validate-content>
	</inbound>
	<!-- Control if and how the requests are forwarded to services  -->
	<backend>
		<base />
	</backend>
	<!-- Customize the responses -->
	<outbound>
		<base />
	</outbound>
	<!-- Handle exceptions and customize error responses  -->
	<on-error>
		<base />
		<return-response>
			<set-status code="@(context.Response.StatusCode)" />
			<set-header name="Content-Type" exists-action="override">
				<value>{{json-type}}</value>
			</set-header>
			<set-body>@{

                    return JsonConvert.SerializeObject(new {
                        status = context.Response.StatusCode,
                        error = context.LastError.Reason,
                        error_message = context.LastError.Message,
                        api_name = context.Api.Name,
                        scope = context.LastError.Scope,
                        section = context.LastError.Section,
                        resource = context.Request.OriginalUrl.ToString(),
                        timezone_ist = TimeZoneInfo.ConvertTimeBySystemTimeZoneId(DateTime.UtcNow, "India Standard Time").ToString("dd-MM-yyyy HH:mm:ss"),
                        correlation = context.Request.Headers.GetValueOrDefault("x-correlation-id", context.RequestId.ToString())
                    });

                }</set-body>
		</return-response>
	</on-error>
</policies>